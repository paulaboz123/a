$schema: https://azuremlschemas.azureedge.net/latest/batchEndpoint.schema.json
name: albot-endpoint-dev
auth_mode: aad_token   # albo: key

# Batch nie ma traffic split. Jeśli chcesz “domyślny”:
defaults:
  deployment_name: en-model
